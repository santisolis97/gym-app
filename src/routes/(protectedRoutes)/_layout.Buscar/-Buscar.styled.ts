import styled from 'styled-components';
import { ScreenWrapper } from '../-layout.styled';

export const BuscarWrapper = styled(ScreenWrapper)`
  gap: 20px;
  justify-content: flex-start;
  padding-top: 10vh;
`;
